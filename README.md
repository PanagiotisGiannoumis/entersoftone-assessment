# Employee Assessment

## Setup
1. `npm install`
2. `npm run dev`
3. Open `http://localhost:5173`

## Notes
- Uses Vite + React + TypeScript + Redux Toolkit + SCSS.
- Mock data stored in `public/employees.json`.
- Search and department filter in the Employee list.
- Click a row to open details modal.
